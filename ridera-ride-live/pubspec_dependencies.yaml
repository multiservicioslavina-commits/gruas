# Agregar a la sección dependencies: de pubspec.yaml
dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.8.0
  geolocator: ^14.0.0
  flutter_foreground_task: ^8.12.0
  flutter_map: ^7.0.2
  latlong2: ^0.9.1
